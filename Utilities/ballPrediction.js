//Still in testing

/*
const BOUNCE_POINTS = 100;
const MAX_POINT = 100;
const MAX_BOUNCE = 5;

const NUM_EXTRA_DOTS = 50;

ig.ENTITY.Crosshair.inject({
	init(x, y, z, settings) {
		this.parent(x, y, z, settings);

		for (let i = 0; i < NUM_EXTRA_DOTS; i++) {
			this._dots.push(ig.game.spawnEntity(ig.ENTITY.CrosshairDot, -1E3,-1E3, 0))
		}
	},

	_updateCrossHair: function(b, e, g, h, i, j, bouncePoints, maxPoint, maxBounce){
		this.parent(b, e, g, h, i, j, BOUNCE_POINTS, MAX_POINT, maxBounce);
	}
});*/

